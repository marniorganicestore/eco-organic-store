---
description: Lean, high-value tests for Eco Organic Store services and the storefront
globs:
  - "**/*Test.java"
  - "**/*Tests.java"
  - "frontend/**/*.test.ts"
  - "frontend/**/*.test.tsx"
alwaysApply: false
---

# Testing

Keep tests lean and behavioral. Prefer one invariant per test. No exhaustive CRUD snapshots. Backend tests are JUnit via Maven Surefire (`./mvnw verify` in CI). Do not add Selenium/Playwright unless asked.

## Must-cover (backend)

- Gateway: unauthenticated `/internal/**` is rejected; valid JWT becomes `X-User-*` headers.
- Inventory: concurrent reserve cannot oversell; release restores `available`.
- Payment: the same Razorpay event id processed twice is idempotent.
- Review: buyer without a qualifying order is rejected.

```java
// ✅ GOOD — invariant
assertThat(second.available()).isEqualTo(originalAvailable);

// ❌ BAD — assert every field of a large Mongo document
```

## Frontend

Zustand cart unit tests (add, qty, empty). Query hooks: mock `fetch`, assert cache key + error mapping.

Browser-verify UX flows instead of E2E suites (shop, checkout, admin).
